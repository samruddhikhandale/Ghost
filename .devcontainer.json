{
    "settings": {
        "editor.formatOnSave": false,
        "python.linting.pylintEnabled": "false"
    },
    "postCreateCommand": "git submodule update --init && yarn setup && npm i grunt -g && echo `whoami` > who.txt"
}
